<?xml version="1.0"?>
<openerp>
  <data>

    <record id="employeee_wage_adjustment" model="ir.rule">
        <field name="name">Normal Employee</field>
        <field name="model_id" ref="model_hr_contract_wage_increment"/>
        <field name="domain_force">[('employee_id.user_id.id', '=', user.id)]</field>
        <field name="groups" eval="[(4,ref('base.group_user'))]"/>
    </record>

      <record id="employeee_wage_adjustment_officer" model="ir.rule">
        <field name="name">View All Adjustment</field>
        <field name="model_id" ref="model_hr_contract_wage_increment"/>
        <field name="domain_force">[(1,'=',1)]</field>
        <field name="groups" eval="[(4,ref('hr_payroll.group_hr_payroll_user'))]"/>
    </record>

  </data>
</openerp>
