<?xml version="1.0" encoding="utf-8" ?>
<openerp>
    <data noupdate="0">
        <!-- Allowed group to export the data. -->
        <record id="group_track_export" model="res.groups">
            <field name="name">Can Export</field>
            <field name="users" eval="[(4, ref('base.user_root'))]"/>
            <field name="comment">the user can export the data from the list view and using the export menu.</field>
        </record>      
    </data>    
</openerp>
