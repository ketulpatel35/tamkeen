<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Trial balance Report -->
    <record id="account_report_trial_balance_view" model="ir.ui.view">
        <field name="name">Trial Balance</field>
        <field name="model">account.balance.report</field>
        <field name="inherit_id" ref="account.account_common_report_view"/>
        <field name="arch" type="xml">
            <data>
                <xpath expr="//field[@name='display_account']" position="after">
                    <field name="initial_balance" invisible="1"/>
                    <newline/>
                </xpath>
                <xpath expr="/form/footer/button[1]" position="after">
                    <button name="trial_report_excel" string="Print in Excel"
                            type="object" class="oe_highlight"/>
                </xpath>
            </data>
        </field>
    </record>

    <record id="action_account_trial_balance_menu"
            model="ir.actions.act_window">
        <field name="name">Trial Balance</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">account.balance.report</field>
        <field name="view_type">form</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="account_report_trial_balance_view"/>
        <field name="target">new</field>
    </record>

    <menuitem id="account.menu_general_Balance_report"
        name="Trial Balance"
        parent="account.menu_finance_legal_statement"
        action="action_account_trial_balance_menu"/>

</odoo>
