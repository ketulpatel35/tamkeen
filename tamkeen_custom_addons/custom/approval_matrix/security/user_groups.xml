<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <data noupdate="0">

        <!-- Approval Matrix -->
        <record model="ir.module.category" id="grp_approval_matrix">
            <field name="name">Approval Matrix</field>
            <field name="sequence">18</field>
        </record>

        <!-- approval matrix user -->
        <!--<record id="group_approval_matrix_user" model="res.groups">-->
            <!--<field name="name">Approval Matrix User</field>-->
            <!--<field name="category_id" ref="grp_approval_matrix"/>-->
        <!--</record>-->

        <!-- Approval Matrix administrator -->
        <record id="group_approval_matrix_administrator" model="res.groups">
            <field name="name">Approval Matrix Administrator</field>
            <field name="category_id" ref="grp_approval_matrix"/>
            <!--<field name="implied_ids"-->
                   <!--eval="[(4, ref('approval_matrix.group_approval_matrix_user'))]"/>-->
        </record>

    </data>
</odoo>
