<?xml version="1.0"?>
<odoo>
  <data>

      <record id="view_record_rule_capture_data" model="ir.rule">
          <field name="name">View All Record Rule for Capture Date</field>
          <field name="model_id"
                 ref="model_attendance_capture_data"/>
          <field name="domain_force">[(1, '=', 1)]
          </field>
          <field name="groups" eval="[(4,ref('group_hr_attendance_view_fingerprint_data'))]"/>
      </record>

  </data>
</odoo>
