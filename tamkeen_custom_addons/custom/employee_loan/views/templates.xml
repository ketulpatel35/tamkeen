<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- loan request send for Manager Approval -->
        <record id="loan_request_send_manager" model="mail.template">
            <field name="name">Employee Loan Manager Approval</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.employee_id.loan_manager_id.work_email | safe}</field>
            <field name="email_cc"></field>
            <field name="subject">Loan Request To Be Approved</field>
            <field name="body_html"><![CDATA[
<p>
Dear ${(object.employee_id.loan_manager_id.user_id.name or '')|safe},
<br/>
<br/>
<p style="padding-left: 4em;">
Kindly, Approve the pending Loan request.
<br/>
    <br/>
<b>Employee Name: </b>${object.employee_id.name}
<br/>
</p>
<br/>
<br/>
% if ctx["display_link"]:
   To proceed, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>
        </record>

        <!-- Loan request send for VP Approval -->
        <record id="loan_pre_req_send_to_vp" model="mail.template">
            <field name="name">Loan VP Approval</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.employee_id.loan_vp_id.work_email | safe}</field>
            <field name="email_cc">${object.employee_id.work_email | safe}</field>
            <field name="subject">Loan Request To Be Approved</field>
            <field name="body_html"><![CDATA[
<p>
Dear ${(object.employee_id.loan_vp_id.user_id.name or '')|safe},
<br/>
<br/>
<p style="padding-left: 4em;">
Kindly, Approve the pending Loan request.
<br/>
<b>Employee Name: </b>${object.employee_id.name}
<br/>
</p>
<br/>
<br/>
% if ctx["display_link"]:
   To proceed, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>
        </record>

        <!-- Budget Approval -->
        <record id="loan_pre_req_send_to_budget" model="mail.template">
            <field name="name">Loan Budget Approval</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.loan_approval_policy_id.budget_email | safe}</field>
            <field name="subject">Loan Budget Approval</field>
            <field name="email_cc">${object.employee_id.work_email | safe}</field>
            <field name="body_html"><![CDATA[
<p>
Dear Budget Team,
<br>
<p style="padding-left: 4em;">
Kindly, Approve the pending Loan request.
<br/>

<b>Employee Name: </b>${object.employee_id.name}
<br/>
<br/>
<br/>
% if ctx["display_link"]:
   To proceed, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>

        </record>

        <!-- loan request send for HR Approval -->
        <record id="loan_pre_req_send_to_hr" model="mail.template">
            <field name="name">Loan HR Approval</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.loan_approval_policy_id.hr_email | safe}</field>
            <field name="subject">Loan Request To Be Approved</field>
            <field name="email_cc">${object.employee_id.work_email | safe}</field>
            <field name="body_html"><![CDATA[
<p>
Dear HR Team,
<br/>
<p style="padding-left: 4em;">
Kindly, Approve the pending Loan request.
<br/>
    <br/>
<b>Employee Name: </b>${object.employee_id.name}
<br/>
</p>
<br/>
<br/>
% if ctx["display_link"]:
   To proceed, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>

        </record>

        <!-- loan request send for CEO Approval -->
        <record id="loan_req_send_to_ceo" model="mail.template">
            <field name="name">Loan CEO Approval</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.employee_id.loan_ceo_id.work_email | safe}</field>
            <field name="subject">Loan Request To Be Approved</field>
            <field name="email_cc">${object.employee_id.work_email | safe}</field>
            <field name="body_html"><![CDATA[
<p>
Dear ${(object.employee_id.loan_ceo_id.user_id.name or '')|safe},
<br/>
<p style="padding-left: 4em;">
Kindly, Approve the pending Loan request.
</p>
<br/>
    <br/>
<b>Employee Name: </b>${object.employee_id.name}
<br/>
</p>
<br/>
<br/>
% if ctx["display_link"]:
   To proceed, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>

        </record>

        <!-- loan request send for Finance Approval -->
        <record id="loan_pre_req_send_to_finance" model="mail.template">
            <field name="name">Loan Finance Approval</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.loan_approval_policy_id.finance_email | safe}</field>
            <field name="subject">Loan Request To Be Approved</field>
            <field name="email_cc">${object.employee_id.work_email | safe}</field>
            <field name="body_html"><![CDATA[
<p>
Dear Finance Team,
<br/>
<p style="padding-left: 4em;">
Kindly, Approve the pending Loan request.
<br/>
    <br/>
<b>Employee Name: </b>${object.employee_id.name}
<br/>
</p>
<br/>
<br/>
% if ctx["display_link"]:
   To proceed, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>

        </record>

        <!-- Loan Request Approved -->
        <record id="loan_req_approved" model="mail.template">
            <field name="name">Loan Request Approved</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.employee_id.work_email | safe}</field>
            <field name="subject">Loan Request has been Approved</field>
            <field name="email_cc">${object.employee_id.work_email | safe}</field>
            <field name="body_html"><![CDATA[
<p>
Dear ${(object.employee_id.name or '')|safe},
<br/>
<br/>
<p style="padding-left: 4em;">
Your loan request has been approved.
<br/>
    <br/>
<br/>
% if ctx["display_link"]:
   To review, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>
        </record>

        <!-- Amount paid to employee from fincnce -->
        <record id="loan_amount_paid_to_employee" model="mail.template">
            <field name="name">Loan Request Paid</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.employee_id.work_email | safe}</field>
            <field name="email_cc">${object.loan_approval_policy_id.hr_email | safe}</field>
            <field name="subject">Loan Request Paid</field>
            <field name="body_html"><![CDATA[
<p>
Dear ${object.employee_id.name},
<br/>
<p style="padding-left: 4em;">
Your loan request has been paid.
<br/>
Kindly, Check your repayment schedule.
<br/>
% if ctx["display_link"]:
   To review, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>
        </record>

        <!-- Loan Request Closed -->
        <record id="loan_req_closed" model="mail.template">
            <field name="name">Loan Request Closed</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <!--<field name="email_from"></field>-->
            <field name="email_to">${object.employee_id.work_email | safe}</field>
            <field name="email_cc">${object.loan_approval_policy_id.hr_email,object.loan_approval_policy_id.finance_email | safe}</field>
            <field name="subject">Loan Request has been Closed</field>
            <field name="body_html"><![CDATA[
<p>
Dear ${(object.employee_id.name or '')|safe},
<br/>
<br/>
<p style="padding-left: 4em;">
Your loan request has been closed.
<br/>
    <br/>
<br/>
% if ctx["display_link"]:
   To review, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>
        </record>

        <!-- loan request Locked -->
        <record id="loan_req_locked_to_employee" model="mail.template">
            <field name="name">Loan Request Locked</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.employee_id.work_email | safe}</field>
            <field name="email_cc">${object.loan_approval_policy_id.hr_email | safe}</field>
            <field name="subject">Loan Request Locked</field>
            <field name="body_html"><![CDATA[
<p>
Dear ${object.employee_id.name},
<br/>
<p style="padding-left: 4em;">
Your loan request has been locked.
<br/>
<br/>
% if ctx["display_link"]:
   To review, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>
        </record>

        <!-- Loan Rejected -->
        <record id="email_template_loan_request_reject" model="mail.template">
            <field name="name">Loan Request Rejected
            </field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.employee_id.work_email | safe}</field>
            <field name="email_cc"></field>
            <field name="subject">Loan Request Rejected</field>
            <field name="body_html"><![CDATA[
<p>
Dear ${object.employee_id.name},
<br/>
<p style="padding-left: 4em;">
Your loan request has been rejected.
<br/>
% if ctx["reason"]:
Refuse Reason: <br/>
" ${ctx["reason"]} "
% endif
<br/>
% if ctx["display_link"]:
   To review, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>
        </record>

        <!-- Loan Cancelled -->
        <record id="email_template_loan_request_cancelled"
                model="mail.template">
            <field name="name">Loan Request Cancelled</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.employee_id.work_email | safe}</field>
            <field name="email_cc"></field>
            <field name="subject">Loan Request Cancelled</field>
            <field name="body_html"><![CDATA[
<p>
Dear ${object.employee_id.name},
<br/>
<p style="padding-left: 4em;">
Your loan request has been cancelled.
<br/>
<br/>
% if ctx["display_link"]:
   To review, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>
        </record>

        <!-- Draft -->
        <record id="email_template_loan_draft" model="mail.template">
            <field name="name">Loan Request Return</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan"/>
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.employee_id.work_email | safe}</field>
            <!-- <field name="email_cc"></field> -->
            <field name="subject">Loan Request Returned</field>
            <field name="body_html"><![CDATA[

<p>
Dear ${object.employee_id.name},
<br>
<p style="padding-left: 4em;">
Your loan request has been returned back to draft state.
<br>
Request Number: ${object.name}
<br>
</p>
</p>
% if ctx["display_link"]:
	To review, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br />
<p> Kind Regards </p>
<br />
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>

			   ]]>

            </field>
        </record>

        <!-- Loan Request Reminder Template -->
        <record id="loan_req_approval_reminder_cron_email_template"
                model="mail.template">
            <field name="name">Loan Request Reminder</field>
             <!--<field name="email_from">${object.loan_approval_policy_id.hr_email}</field>-->
            <field name="subject">Approval Reminder</field>
            <field name="email_to">${ctx["email_to"] | safe}</field>
            <field name="model_id"
                   ref="employee_loan.model_hr_employee_loan" />
            <field name="auto_delete" eval="True"/>
            <field name="body_html"><![CDATA[
Dear ,
<br>
Please take a moment to approve the pending ${(object.name or '')|safe} Loan Request.
<br>
Employee Name: ${object.employee_id.name}
<br>
Number: ${object.name}
<br>
% if ctx["display_link"]:
   To proceed, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br />
<p> Kind Regards </p>
<br />
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
            ]]></field>
        </record>

        <!-- Loan Installment Email -->

        <!-- Ask for Reschedule -->
        <record id="loan_inst_ask_for_reschedule"
                model="mail.template">
            <field name="name">Loan Installment Ask for Reschedule</field>
             <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.loan_id.loan_approval_policy_id.ask_for_reschedule_email | safe}</field>
            <field name="subject">Ask for Rescheduling a Loan Installment</field>
            <field name="model_id"
                   ref="employee_loan.model_loan_installments" />
            <field name="auto_delete" eval="True"/>
            <field name="body_html"><![CDATA[
<br>
Please take a moment to respond to the employee request for rescheduling one of the installments.
<br>
Employee Name: ${object.employee_id.name}
<br>
Reference: ${object.loan_id.name}
<br>
% if ctx["display_link"]:
   To proceed, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br />
<p> Kind Regards </p>
<br />
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
            ]]></field>
        </record>

        <!-- loan installment send for Finance Approval -->
        <record id="loan_inst_req_send_to_finance" model="mail.template">
            <field name="name">Loan Installment for Finance Approval</field>
            <field name="model_id"
                   ref="employee_loan.model_loan_installments" />
            <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.loan_id.loan_approval_policy_id.finance_email | safe}</field>
            <field name="subject">Loan Installment To Be Approved</field>
            <field name="body_html"><![CDATA[
<p>
Dear Finance Team,
<br/>
<p style="padding-left: 4em;">
Kindly, Approve the pending Loan Installment.
<br/>
    <br/>
<b>Employee Name: </b>${object.employee_id.name}
<br/>
</p>
<br/>
<br/>
% if ctx["display_link"]:
   To proceed, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>

        </record>

        <!-- Installment Rescheduled -->
        <record id="loan_inst_reschedule" model="mail.template">
            <field name="name">Loan Installment Reschedule</field>
             <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.employee_id.work_email | safe}</field>
            <field name="subject">Rescheduled a Loan Installment</field>
            <field name="model_id"
                   ref="employee_loan.model_loan_installments" />
            <field name="auto_delete" eval="True"/>
            <field name="body_html"><![CDATA[
<p>
Dear ${object.employee_id.name},
<br/>
<p style="padding-left: 4em;">
Your loan installment has been rescheduled.
<br/>
<br/>
% if ctx["display_link"]:
   To review, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>
        </record>

                <!-- Installment Rescheduled Reject -->
        <record id="loan_inst_rescheduling_reject" model="mail.template">
            <field name="name">Loan Installment Rescheduling Rejected</field>
             <field name="email_from">${user.email | safe}</field>
            <field name="email_to">${object.employee_id.work_email | safe}</field>
            <field name="subject">Loan Installment Rescheduling Rejected</field>
            <field name="model_id"
                   ref="employee_loan.model_loan_installments" />
            <field name="auto_delete" eval="True"/>
            <field name="body_html"><![CDATA[
<p>
Dear ${object.employee_id.name},
<br/>
<p style="padding-left: 4em;">
Your loan installment rescheduling has been rejected.
<br/>
<br/>
% if ctx["display_link"]:
   To review, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br/>
<p> Kind Regards </p>
<br/>
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
			   ]]>
            </field>
        </record>

        <!-- Loan Installment Reminder Templet -->
        <record id="loan_inst_ask_for_reschedule_reminder"
                model="mail.template">
            <field name="name">Loan Installment Approval Reminder</field>
             <!--<field name="email_from">${object.loan_id.loan_approval_policy_id-->
             <!--.hr_email}</field>-->
            <field name="subject">Loan Installment Approval Reminder</field>
            <field name="email_to">${ctx["email_to"] | safe}</field>
            <field name="model_id"
                   ref="employee_loan.model_loan_installments" />
            <field name="auto_delete" eval="True"/>
            <field name="body_html"><![CDATA[
<br>
Please take a moment to approve the pending Loan Installment Approval.
<br>
Employee Name: ${object.employee_id.name}
<br>
Reference: ${object.loan_id.name}
<br>
% if ctx["display_link"]:
   To proceed, Kindly <a href="${ctx["base_url"]}/web#id=${object.id}&view_type=form&model=${ctx["model"]}&action=${ctx["action_id"]}">Click Here</a>
% endif
<br />
<p> Kind Regards </p>
<br />
<p style="font-size:13px; color:blue; border: 1px solid red; width:350px;"> Auto generated message through the ERP System. </p>
            ]]></field>
        </record>

    </data>
</odoo>